# AGENTS.md

## 1. 文件作用

本文件是项目0的 AI / Codex 协作入口规则文件，用于说明 Agent 进入仓库前必须遵守的阶段、边界、红线和协作流程。

所有 AI、Codex、GPT、Opus、CherryStudio 在参与项目0规划、审查、修改文档或修改代码前，都应先阅读本文件。

本文件承接并指向以下治理入口，不重复完整写入其细则：
1. `docs/_meta/conventions/ai_workflow.md`：日常 AI 使用流程细则；
2. `docs/_meta/conventions/document_governance.md`：文档事实源、颗粒度、状态和修改边界细则；
3. `docs/DOC_INDEX.md`：文档索引与依赖导航；
4. `process/change_requests/`：跨文件修改入口。

---

## 2. 当前阶段

当前仓库状态：G0 已正式收束，已形成可支撑 P1.3 回归和后续治理的最小治理基线；当前主线回到 P1.3 硬件方案与采购。

G0 结束不等于所有遗留问题清零。后续问题按现有 CR / 阶段任务机制承接，不新增专门的后续治理跟踪文件。

当前项目主线阶段：P1.3 硬件方案与采购。

P1.3 阶段重点：
1. 已有硬件盘点与状态确认；
2. 缺失硬件确定；
3. BOM 与采购清单输出；
4. 启动采购。

当前不应提前大规模展开 P2-P6 代码实现，也不应提前展开 P7-P10 迭代二研究增强细节。

---

## 3. 项目0总原则

1. 先做成，再做强，再做深。
2. 迭代一先独立完成并验收，再进入迭代二。
3. 真机主线，仿真辅助，真机验收。
4. 单人 solo 开发，每阶段可执行、可验收、可回退。
5. 工程优先，研究兼顾。
6. 平台成立优先于高复杂度炫技。
7. 安全底线优先于性能和花哨功能。
8. 范围收敛优先于功能膨胀。
9. 成果表达与素材采集贯穿全程。
10. 已有硬件应服务于项目目标，而不是反向绑死项目方向。

---

## 4. 当前禁止事项

Codex / AI 不得自行执行以下行为：

1. 不得推翻已确认的迭代一 / 迭代二阶段路线。
2. 不得把迭代二增强内容提前写成迭代一必须实现项。
3. 不得擅自修改已确认的 DEC 决策结论。
4. 不得把项目0扩展为机械臂、复杂抓取、多机器人、无人机、人形机器人或纯仿真项目。
5. 不得一次性重写整个 docs/ 目录。
6. 不得在没有影响分析的情况下跨多个文件批量修改。
7. 不得削弱安全机制、人工接管、急停、系统模式仲裁、电源边界、风险区域约束等安全底线。
8. 不得在未完成 P1.3 BOM / 采购清单人工确认前直接采购，或写成采购已完成。
9. 不得擅自修改 Architecture 技术事实、通信链路事实或已确认的 DEC 结论。

---

## 5. AI / Codex 工作方式

所有跨文件修改必须遵循：

Plan → Execute → Verify → Commit

含义：

1. Plan：只读分析，列出影响范围，不修改文件。
2. Execute：经人工确认后，只修改批准清单中的文件。
3. Verify：修改后检查是否存在矛盾、越界、遗漏。
4. Commit：人工查看 diff 后，再提交到 Git。

涉及跨文件修改时，必须先建立或引用 `process/change_requests/` 中的 CR，并遵守 CR 明确的允许修改范围和禁止修改范围。

日常 AI / Codex 使用流程细则以 `docs/_meta/conventions/ai_workflow.md` 为准。

---

## 6. 文档治理原则

1. 单一事实源：同一事实只在一个权威位置定义。
2. 下游文档只引用或承接，不重复定义上游事实。
3. 决策理由优先记录在 process/decisions/DEC-XXX 文件中。
4. 跨文件变更必须建立或引用 process/change_requests/。
5. 重要版本变化应更新 CHANGELOG.md。
6. 接口变更必须检查 interface_definition.md、相关设计文档和 CHANGELOG.md。

文档事实源、颗粒度、状态和修改边界细则以 `docs/_meta/conventions/document_governance.md` 为准；文档导航与依赖关系以 `docs/DOC_INDEX.md` 为准。

---

## 7. 当前优先级

当前最高优先级不是“重写所有文档”，而是：

1. 保护当前 Git 状态；
2. 使用 AI 工作流规则与文档治理规则约束后续修改；
3. 使用变更请求机制管理跨文件变更；
4. 使用现有 CR / 阶段任务机制承接 G0 遗留问题；
5. 回到 P1.3 主线，完成 BOM 与采购清单。
