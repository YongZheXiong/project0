# 当前 H60 + MC520 两层整车 CAD

## 资产状态

- 文件数：28 个 STEP
- STEP 总大小：`196428077 bytes`
- 数字复审：28 / 28 通过 OCP 7.7.2 B-Rep 有效性检查
- 制造状态：两层铝板、D435 支架和 Mid360 平台已委托加工；尚未交付、试装或验收

## 当前设计口径

- 轴距：`170 mm`
- 轮距：约 `184.16 mm`
- 轮胎最外侧：约 `210.20 mm`
- 整车数字包络：约 `319.05 × 210.20 × 210.07 mm`（长 × 宽 × 高）
- 第一层底板：`290 × 189.4 × 3 mm` 单一实体，电机支架限位凸起已删除
- 层间支撑：四角与前后中部共 6 根支柱
- D435：与地面平行安装
- 静态最低非轮结构离地：约 `10.67 mm`

修复后的 Orin NX、第二层装配和整车总装均通过 B-Rep 有效性检查。上述数值来自数字模型，不包含轮胎压缩、紧固件突出、加工公差、线束或真实载荷变形。

## 验证边界

收货后仍需不上电核对材质、厚度、孔径/孔距、平面度、打印尺寸和方向，并完成试装、紧固防松、维护空间、质量/重心、动态离地间隙以及 D435/Mid360 真实数据遮挡验证。数字几何通过不代表电气、上电或运动准入。

`MANIFEST.sha256` 用于验证公开 STEP 的逐文件完整性。STEP 保留原始导出字节，不做源码式空白清理。

## 平台命名边界

`OpenCTR H60 主控板.step`、`新第二层底盘装配.step` 和 `新小车整体装配.step` 内部出现 `STM32F407VET6-LQFP100-14x14` 组件名。它是 H60 板载 MCU 的 CAD 元件名称，不表示上一平台的独立 STM32 控制板仍在当前装配中。当前 28 个 STEP 未检出 `WSDC`、`JGB37/JGB-520` 或 `83/85 mm` 旧轮系组件名。
