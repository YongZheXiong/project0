# Chassis Layer 2 CAD

状态：历史验证资料。第二层底板与电驱下挂布局验证入口已保留，但不再作为当前最终加工基线。

当前最终加工基线已由 `../chassis_three_layer_freeze_v0_1/` 承接。本目录仅用于追溯早期第二层布局验证过程。

本目录用于记录 Project0 第二层底板的设计输入、布局基准和后续 CAD 草模。当前阶段仅用于验证第二层能否容纳剩余硬件、下挂两块电驱，以及与第一层底板、立柱、线束和开关空间的关系。

当前文件：

1. `design_inputs.md`
   - 第二层底板 CAD 建模参数输入草案。

2. `layout_baseline_v0_1.md`
   - 第二层布局基准草案。

说明：

1. 本目录不是最终加工图目录。
2. 第二层底板外形、层间高度、立柱孔位和硬件孔位均待 Fusion 360 装配验证。
3. 当前优先验证两块电驱安装在第二层底板下方，以释放第一层中部两侧空间。
4. 第一层底板长度下一轮优先按 `290 mm` 验证，`300 mm` 作为更宽裕备选。


