# Emergency Stop Button v0.1

状态：v0.1 单件 CAD 资产已归档，用于急停按钮尾部空间和安装位置验证。

## 文件

1. `source/emergency_stop_button_v0_1.f3d`：Fusion 360 可编辑源文件。
2. `final/emergency_stop_button_v0_1.step`：STEP 交换文件，后续装配优先使用。
3. `preview/emergency_stop_button_v0_1.stl`：STL 预览和包围盒检查文件。

## 来源

1. 原始导出目录：`C:/Users/Gdsdc/OneDrive/桌面/111122/`
2. 原始文件名前缀：`急停按钮`
3. 建模方式：用户在 Fusion 360 中按实物外形建模并导出。

## 尺寸检查

1. STL 三角面数量：`10842`。
2. STL 包围盒约为 `39.999 x 76.5 x 41 mm`。
3. STEP 单位为 mm。
4. STEP 检测到 `1` 个实体 BREP、`1` 个 closed shell 和 `49` 个 advanced face。

## 使用说明

1. 用于第二层尾部急停按钮安装位置、触点模块尾部深度和线束避让验证。
2. 急停按钮应保持易触达，不应被传感器、上层支架或外壳遮挡。
3. 当前模型不替代急停电气安全边界和继电器切断方案。


