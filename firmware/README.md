# Firmware Workspace

本目录承接 Project0 当前 H60 固件需求，以及上一 STM32 平台的固件和实测历史。

当前入口包括 `h60_safe_control/` 与 `h60_motion_control_firmware_requirements_v0_2.md`。`h60_safe_control` 已提供并验证 `v0.1.1` 运动锁定固件：默认 `DISARMED`、显式 ARM 拒绝、协议边界、通信超时、IWDG 和异常撤 PWM；生产构建固定 `MOTION=0`，不配置 PWM，也不放行接电机或运动。反转斜坡、闭环和动力验收仍待实现。

以下 STM32 记录均为上一实车平台历史，不适用于 H60 引脚、协议、电机方向、编码器 CPR 或新平台验收：

上一 STM32 平台已有记录：

| 文件 | 作用 | 状态 |
|---|---|---|
| `stm32_safe_init_v0_1.md` | 记录 P2 电驱控制端安全初始化工程、GPIO 分配、编译/调试路径和实测结果 | 已完成阶段记录 |
| `stm32_lf_single_motor_pulse_test_v0_1.md` | 记录左前 `LF` 单电机低占空比短脉冲测试的临时固件行为、诊断过程和测试结果 | 已完成阶段记录 |
| `stm32_four_single_motor_pulse_tests_v0_1.md` | 记录 `LF / LR / RF / RR` 四个单电机低占空比短脉冲测试、右侧实物映射、主输入保险故障处理和测试后安全回烧 | 已完成阶段记录 |
| `stm32_suspended_linkage_pulse_tests_v0_1.md` | 记录悬空同侧双电机联动、四轮联动、右侧方向补偿、`LF` 起转门槛诊断和测试后安全回烧 | 已完成阶段记录 |
| `stm32_uart3_minimal_test_v0_1.md` | 记录 USART3 `PB10/PB11` 最小 UART 测试固件、双向 ACK、烧录和重启 / 冷启动复验 | 已完成阶段记录 |
| `stm32_encoder_acceptance_v0_1.md` | 记录旧平台新映射安全输出、四路编码器手动转轮、交叉测试、`LF` 故障定位和替换后复验 | 四路编码器低功率手动转轮通过 |

历史记录注意：

1. 旧 STM32CubeIDE 工程保留在非公开本地工作区，未纳入公开仓库源码管理；公开文档只保留可复核的历史结果摘要；
2. 仓库内 `stm32_safe_init_v0_1.md` 只记录该工程的事实、关键代码行为和验证结果；
3. `stm32_lf_single_motor_pulse_test_v0_1.md` 只记录一次左前单电机临时测试；
4. `stm32_four_single_motor_pulse_tests_v0_1.md` 记录四个单电机测试均已通过；`stm32_suspended_linkage_pulse_tests_v0_1.md` 继续记录悬空同侧联动、四轮联动和方向补偿结果；
5. 2026-06-22 的旧平台方向重映射作为历史结果保留；完整针序未在当前公开快照展开，不得迁移到 H60；
6. 当前悬空方向一致性已通过，但仅在悬空、短时、人工看守条件下成立，不表示整车落地运动已完成；
7. 原 STM32 在故障发生前最后一次已知烧录为安全初始化固件，现已因 3.3V 和 `RESET / NRST` 异常保持隔离；替换板已完成供电、烧录和 USART3 双向验收；
8. 2026-06-16 已补记 USB / 调试器反供电现象：主开关 OFF 但 STM32 USB 插着时，电驱端曾约 `4.16V`；拔掉 USB 后为 `0V`，不应仅凭电驱灯判断主动力状态；
9. `stm32_uart3_minimal_test_v0_1.md` 只用于 Orin X / Orin NX 与 STM32 UART 物理链路验证，烧录前第 4 路动力保险应保持拔出；
10. 2026-06-20 已完成替换板 USART3 最小双向 ACK、Orin 重启和 Orin / STM32 控制支路断电冷启动复验；正式控制协议当时仍未完成；
11. `stm32_safe_init_v0_1.md` 和既有短脉冲测试记录基于旧右侧引脚，只作为历史事实；改线后禁止直接使用旧固件恢复动力；
12. 2026-06-22 新映射固件已完成编译、烧录、安全输出、UART 和编码器读取测试；`LR / RF / RR` 编码器通过，`LF` 经交叉测试定位为电机内置编码器 / 内部连接器 / 原厂尾线侧故障；
13. 2026-07-13，`LF` 替换电机装车后重新压紧编码器蓝线 / 黑线并复验通过，四路编码器在第 4 路动力保险空置、人工手动转轮、UART 读数条件下均可产生有效计数；
14. 2026-07-13，旧 Windows STM32 工程已通过 U 盘取回，新 Mac 可构建当前 `project0_stm32_uart3_test`，车上 flash 已只读备份，active 固件前缀与旧 Windows 工程产物、新 Mac 构建产物一致；
15. 2026-07-14，`project0_stm32_uart3_test` 已加入阶段 D 前置控制层：新增 `STOP / SAFE / PULSE / DRIVE` 文本命令、`300ms` 通信超时停车、非法命令停车、`250ms` 短脉冲上限、`20 / 55` compare 上限和急停 `NO` 反馈状态；当日烧录 ELF SHA-256 为 `eb4d9c21ad2c4331132bc7258b529735dcae35e5e25987500af1212eea817ca5`；
16. 2026-07-14，用户确认第 4 路动力保险拔出后，阶段 D 前置控制固件已通过 OpenOCD 烧录到车上替换 STM32 并 `Verified OK`；Orin `/dev/ttyTHS1` 默认低功率 UART 复验 `overall=PASS`，未发送 `PULSE / DRIVE` 主动运动命令；当前观测到 `ESTOP=1`，需在阶段 D 前人工确认急停按钮状态和 `NO` 反馈转换；
17. 2026-07-17，急停按下 / 释放的 `ESTOP=1/0` 转换、急停动力切断、四路悬空前进 / 后退短脉冲和自动停车通过；诊断期间发现左后 `LR` 不动作，交叉测试证明电机与编码器正常，最终定位为 `set_left_forward()` 遗漏 `PD2 / PD3`；
18. 左后方向输出修复后的 ELF SHA-256 为 `583880a87b278a84922d24435b415d90d7ea15c16e880d584ec80ce4fb99e596`，OpenOCD `Verified OK`；动力保险拔出时正向输入实测 `INA2=0.17V`、`INB2≈3.3V`，恢复动力后的 `FWD / REV 12/55 200ms` 动作和编码器方向通过；
19. 2026-07-17，悬空 `LEFT / RIGHT 12/55 200ms`、单次 `DRIVE` 无新指令 `300ms` 超时和通信客户端中断后独立停车通过；形成原始编码器符号表，后续里程计应保留左侧符号并对右侧计数取反；
20. 阶段 D 悬空低层运动与安全停车验收完成；测试后急停按下、整车断电并拔出第 4 路动力保险。该结果不等同于里程计、ROS2 teleop、人工接管仲裁或阶段 E 落地运动通过。
